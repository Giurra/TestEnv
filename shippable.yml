# language setting
language: java

# jdk tag
jdk:
 - openjdk7
 - oraclejdk7
 - openjdk6
 - oraclejdk8

# Maven
#install:
#mvn install -DskipTests=true;

addons:
   firefox: "44.0"
services:
  - selenium
before_script:
  - "export DISPLAY=:99.0"
  - "/etc/init.d/xvfb start"
after_script:
  - "/etc/init.d/xvfb stop"
  
  npm test